# Skeleton Analyzer 라이센스 시스템

> 📅 마지막 갱신: 2026-02-01
> 🔍 소스: 코드베이스 자동 분석

## 개요

Skeleton Analyzer는 하드웨어 기반 라이센스 시스템을 사용합니다. 각 컴퓨터의 고유 하드웨어 ID를 기반으로 라이센스 키가 발급되며, 해당 컴퓨터에서만 라이센스가 유효합니다.

## 라이센스 모드

| 모드 | 설명 | 기능 |
|------|------|------|
| FREE | 무료 모드 | 기본 기능만 사용 가능 |
| LICENSED | 등록 모드 | 모든 기능 사용 가능 |
| DEV | 개발 모드 | 모든 기능 사용 가능 (개발용) |

## 기능별 라이센스 요구사항

### 무료 기능 (FREE)

- 동영상 로드 및 재생
- RULA 분석
- REBA 분석
- OWAS 분석
- 프레임 캡처

### 유료 기능 (LICENSED)

- NLE 분석 (NIOSH Lifting Equation)
- SI 분석 (Strain Index)
- 프로젝트 저장
- 프로젝트 열기
- Excel 내보내기
- JSON 내보내기

## 하드웨어 ID

시스템 고유 식별자를 SHA-256 해시로 변환한 16자리 대문자 16진수입니다.

### 플랫폼별 획득 방법

| 플랫폼 | 소스 |
|--------|------|
| macOS | `system_profiler` Hardware UUID |
| Windows | WMIC 또는 PowerShell 시스템 UUID |
| Linux | `/etc/machine-id` 또는 DMI UUID |

### 폴백

플랫폼별 ID 획득 실패 시 MAC 주소 기반 UUID를 사용합니다.

## 라이센스 키 형식

```
XXXX-XXXX-XXXX-XXXX
```

- **1-2번째 그룹 (8자리)**: 하드웨어 ID 해시
- **3번째 그룹 (4자리)**: 제품 코드 (`SA25`)
- **4번째 그룹 (4자리)**: 체크섬

## 검증 과정

1. **형식 검증**: `XXXX-XXXX-XXXX-XXXX` 패턴 확인
2. **체크섬 검증**: SHA-256 기반 무결성 확인
3. **하드웨어 매칭**: 현재 시스템 하드웨어 ID와 비교

## 라이센스 저장 위치

| 플랫폼 | 경로 |
|--------|------|
| Windows | `%APPDATA%\SkeletonAnalyzer\license.json` |
| macOS/Linux | `~/.config/SkeletonAnalyzer/license.json` |

### license.json 구조

```json
{
  "license_key": "XXXX-XXXX-XXXX-XXXX",
  "hardware_id": "ABCDEF1234567890",
  "checksum": "무결성 체크섬"
}
```

## 환경변수

개발 및 테스트 목적으로 환경변수를 통해 라이센스 모드를 설정할 수 있습니다.

| 환경변수 | 값 | 설명 |
|----------|-----|------|
| `SKELETON_ANALYZER_LICENSE_MODE` | `dev` | 개발 모드 활성화 |
| | `licensed` | 강제 등록 모드 |
| | `free` 또는 없음 | 기본 무료 모드 |

### 사용 예시

```bash
# macOS/Linux
export SKELETON_ANALYZER_LICENSE_MODE=dev
python main.py

# Windows
set SKELETON_ANALYZER_LICENSE_MODE=dev
python main.py
```

## 라이센스 등록 UI

`도움말(H)` > `라이센스 등록(L)...` 메뉴에서 라이센스 등록 다이얼로그를 열 수 있습니다.

### 등록 절차

1. 다이얼로그에서 **하드웨어 ID** 복사
2. 관리자에게 하드웨어 ID 전달
3. 발급받은 라이센스 키 입력
4. **등록** 버튼 클릭

### 등록 해제

등록된 상태에서 **등록 해제** 버튼을 클릭하면 라이센스가 해제됩니다.

## 키 생성 도구

관리자용 라이센스 키 생성 도구가 `tools/license_keygen.py`에 포함되어 있습니다.

### 사용법

```bash
# 단일 키 생성
python tools/license_keygen.py ABCDEF1234567890

# 배치 생성
python tools/license_keygen.py --batch hardware_ids.txt --output keys.txt

# 키 검증
python tools/license_keygen.py --validate XXXX-XXXX-XXXX-XXXX ABCDEF1234567890
```

### 옵션

| 옵션 | 설명 |
|------|------|
| `<hardware_id>` | 단일 하드웨어 ID로 키 생성 |
| `--batch, -b <file>` | 하드웨어 ID 목록 파일로 배치 생성 |
| `--validate, -v <key> <hwid>` | 키 유효성 검증 |
| `--output, -o <file>` | 결과 출력 파일 (배치 모드) |

## 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    LicenseManager                        │
│                     (싱글톤)                             │
├─────────────────────────────────────────────────────────┤
│  - license_key: Optional[str]                           │
│  - mode: LicenseMode                                    │
│  + instance() → LicenseManager                          │
│  + is_licensed → bool                                   │
│  + check_feature(name) → bool                           │
│  + register(key) → bool                                 │
│  + unregister()                                         │
├─────────────────────────────────────────────────────────┤
│                         │                               │
│    ┌────────────────────┼────────────────────┐         │
│    ▼                    ▼                    ▼         │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐    │
│ │ HardwareId   │ │LicenseValidator│ │ LicenseDialog │   │
│ │              │ │              │ │              │    │
│ │get_hardware_ │ │ validate()   │ │ (PyQt6 UI)   │    │
│ │   id()       │ │ normalize()  │ │              │    │
│ └──────────────┘ └──────────────┘ └──────────────┘    │
└─────────────────────────────────────────────────────────┘
```

## 보안 고려사항

- 라이센스 파일은 체크섬으로 무결성 검증
- 하드웨어 ID는 해시 처리되어 원본 노출 방지
- 라이센스 키 생성 로직은 시크릿 키 사용

## 관련 파일

| 파일 | 설명 |
|------|------|
| `src/license/__init__.py` | 모듈 공개 API |
| `src/license/hardware_id.py` | 하드웨어 ID 생성 |
| `src/license/license_validator.py` | 키 검증 로직 |
| `src/license/license_manager.py` | 상태 관리 (싱글톤) |
| `src/license/license_dialog.py` | 등록 UI (PyQt6) |
| `src/ui/components/license_overlay.py` | 기능 제한 오버레이 |
| `tools/license_keygen.py` | 관리자용 키 생성 도구 |

## 테스트

라이센스 시스템 테스트 코드는 `tests/license/` 디렉토리에 있습니다.

```bash
# 라이센스 테스트 실행
pytest tests/license/ -v
```
