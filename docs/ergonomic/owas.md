# OWAS (Ovako Working Posture Analysis System)

> 📅 마지막 갱신: 2026-01-30

## 개요

OWAS는 핀란드 Ovako Oy Steel Company에서 1977년에 개발한 작업 자세 분석 시스템입니다. 간단한 4자리 자세 코드를 사용하여 작업 자세의 유해성을 빠르게 평가합니다.

## 평가 구조

OWAS는 4자리 자세 코드로 작업 자세를 표현합니다:

```
[등][팔][다리][하중]
  1   2   3    4
```

예: `2121` = 등 굴곡(2) + 양팔 어깨 아래(1) + 양다리 서기(2) + 10kg 미만(1)

## 자세 코드 상세

### 1자리: 등 (Back) - 1~4

| 코드 | 자세 | 설명 |
|------|------|------|
| 1 | 직립 | 곧게 선 상태 |
| 2 | 굴곡 | 앞으로 구부린 상태 |
| 3 | 회전/측굴 | 몸통이 비틀리거나 옆으로 기울어진 상태 |
| 4 | 굴곡+회전 | 구부리면서 비틀린 상태 |

### 2자리: 팔 (Arms) - 1~3

| 코드 | 자세 | 설명 |
|------|------|------|
| 1 | 양팔 어깨 아래 | 두 팔 모두 어깨 높이 아래 |
| 2 | 한 팔 어깨 위 | 한 팔이 어깨 높이 이상 |
| 3 | 양팔 어깨 위 | 두 팔 모두 어깨 높이 이상 |

### 3자리: 다리 (Legs) - 1~7

| 코드 | 자세 | 설명 |
|------|------|------|
| 1 | 앉기 | 앉아있는 상태 |
| 2 | 양다리 서기 | 두 다리로 균형 있게 서 있음 |
| 3 | 한다리 서기 | 한 다리에 체중이 실린 상태 |
| 4 | 양무릎 굴곡 | 두 무릎이 구부러진 상태 |
| 5 | 한무릎 굴곡 | 한 무릎이 구부러진 상태 |
| 6 | 무릎꿇기 | 무릎을 꿇은 상태 |
| 7 | 걷기/이동 | 이동 중인 상태 |

### 4자리: 하중 (Load) - 1~3

| 코드 | 무게 | 설명 |
|------|------|------|
| 1 | 10kg 미만 | 가벼운 하중 |
| 2 | 10~20kg | 중간 하중 |
| 3 | 20kg 초과 | 무거운 하중 |

> **참고**: Skeleton Analyzer에서는 하중 정보를 자동으로 감지할 수 없어 기본값 1을 사용합니다.

## 조치 카테고리 (Action Category)

자세 코드 조합에 따라 4가지 조치 카테고리(AC)로 분류됩니다:

| AC | 위험 수준 | 색상 | 조치 |
|----|-----------|------|------|
| AC1 | 정상 (normal) | 🟢 초록 | 조치 불필요 - 정상적인 작업 자세 |
| AC2 | 약간 유해 (slight) | 🟡 노랑 | 가까운 시일 내 개선 필요 |
| AC3 | 명백히 유해 (harmful) | 🟠 주황 | 가능한 빨리 개선 필요 |
| AC4 | 매우 유해 (very_harmful) | 🔴 빨강 | 즉시 개선 필요 |

### AC 결정 테이블 (일부)

등-팔-다리 조합에 따른 조치 카테고리:

| 등 | 팔 | 다리 1-7 | AC |
|----|----|---------|----|
| 1 (직립) | 1-3 | 모두 | AC1 |
| 2 (굴곡) | 1-2 | 1-5 | AC2 |
| 2 (굴곡) | 3 | 6-7 | AC3 |
| 3 (회전) | 1 | 1-5 | AC1 |
| 3 (회전) | 2-3 | 대부분 | AC2 |
| 4 (굴곡+회전) | 1 | 1-2 | AC2 |
| 4 (굴곡+회전) | 3 | 3-7 | AC4 |

## 코드 구조

### OWASCalculator (`src/core/ergonomic/owas_calculator.py`)

```python
class OWASCalculator(BaseAssessment):
    """OWAS 점수 계산기"""

    # 조치 카테고리 테이블
    ACTION_CATEGORY_TABLE = {
        (1, 1, 1): 1, (1, 1, 2): 1, ...  # (등, 팔, 다리): AC
        (4, 3, 7): 4,
    }

    def calculate(self, angles: Dict[str, float], landmarks: List[Dict]) -> OWASResult:
        """OWAS 점수 계산"""
        # 각 부위 코드 계산
        back_code = self._calculate_back_code(angles, landmarks)
        arms_code = self._calculate_arms_code(angles, landmarks)
        legs_code = self._calculate_legs_code(angles, landmarks)
        load_code = 1  # 기본값

        # 자세 코드 생성
        posture_code = f"{back_code}{arms_code}{legs_code}{load_code}"

        # 조치 카테고리 결정
        action_category = self._get_action_category(back_code, arms_code, legs_code)

        return OWASResult(...)
```

### OWASResult (`src/core/ergonomic/owas_calculator.py`)

```python
@dataclass
class OWASResult(AssessmentResult):
    """OWAS 평가 결과"""
    back_code: int = 1        # 등 코드 (1-4)
    arms_code: int = 1        # 팔 코드 (1-3)
    legs_code: int = 1        # 다리 코드 (1-7)
    load_code: int = 1        # 하중 코드 (1-3)
    action_category: int = 1  # 조치 카테고리 (1-4)
    posture_code: str = "1111"  # 4자리 자세 코드
```

### OWASWidget (`src/ui/ergonomic/owas_widget.py`)

```python
class OWASWidget(QWidget):
    """OWAS 평가 결과 표시 위젯"""

    # 색상 정의
    COLORS = {
        'normal': '#4CAF50',        # 초록
        'slight': '#FFC107',        # 노랑
        'harmful': '#FF9800',       # 주황
        'very_harmful': '#F44336',  # 빨강
    }

    # 자세 설명
    BACK_DESCRIPTIONS = {1: '직립', 2: '굴곡', 3: '회전/측굴', 4: '굴곡+회전'}
    ARMS_DESCRIPTIONS = {1: '양팔 어깨 아래', 2: '한 팔 어깨 위', 3: '양팔 어깨 위'}
    LEGS_DESCRIPTIONS = {
        1: '앉기', 2: '양다리 서기', 3: '한다리 서기',
        4: '양무릎 굴곡', 5: '한무릎 굴곡', 6: '무릎꿇기', 7: '걷기/이동'
    }

    def update_result(self, result: OWASResult):
        """결과 업데이트"""
        # 자세 코드 (콤마로 구분), AC, 각 코드 설명 표시
```

## UI 표시 형식

OWAS 결과는 다음과 같이 표시됩니다:

```
    자세 코드: 2,1,2,1
    AC: AC2
    위험 수준: 약간 유해

    자세 코드 상세:
    등 (1자리): 2 - 굴곡
    팔 (2자리): 1 - 양팔 어깨 아래
    다리 (3자리): 2 - 양다리 서기
    하중 (4자리): 1 - 기본값
```

## RULA/REBA와의 차이점

| 항목 | RULA | REBA | OWAS |
|------|------|------|------|
| 평가 방식 | 점수 조합 | 점수 조합 | 코드 조합 |
| 결과 형식 | 1-7점 | 1-12점 | AC1-AC4 |
| 세부 수준 | 높음 | 높음 | 낮음 (빠른 평가) |
| 하중 고려 | 간접 | 간접 | 직접 (4자리 코드) |
| 적용 분야 | 사무직, 제조업 | 의료, 다양한 산업 | 제조업, 중공업 |

## 참고 문헌

- Karhu, O., Kansi, P., & Kuorinka, I. (1977). Correcting working postures in industry: a practical method for analysis. *Applied Ergonomics*, 8(4), 199-201.
- Karhu, O., Härkönen, R., Sorvali, P., & Vepsäläinen, P. (1981). Observing working postures in industry: Examples of OWAS application. *Applied Ergonomics*, 12(1), 13-17.
